<%= content_for(:title, "SpinOFF") %>

<div class="flex flex-col justify-center w-96">
  <%= link_to("Voltar", root_path) %>

  <div class="flex items-center justify-center">
    <%= image_tag("rubyrita.png", width: 200) %>
  </div>

  <h1 class="font-bold text-xl h-32">
    Apenas um frevo on rails SpinOFF
  </h1>
</div>
